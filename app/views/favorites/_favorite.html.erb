<% if favorite %>
  <%= link_to "-1", post_favorite_path(@post, favorite), :method => :delete, :class => "btn btn-danger", :remote => true %>
<% else %>
  <%= link_to "+1", post_favorites_path(@post), :method => :post, :class => "btn btn-primary", :remote => true %>
<% end %>